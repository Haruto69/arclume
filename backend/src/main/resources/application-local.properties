# Explicit local-development profile. These defaults must never be used in production.
app.security.auth.challenge-secret=${AUTH_CHALLENGE_SECRET:arclume-local-only-challenge-secret-at-least-32-bytes}
app.security.auth.encryption-key=${AUTH_ENCRYPTION_KEY:arclume-local-only-encryption-key-at-least-32-bytes}
app.security.auth.cookie.secure=${AUTH_COOKIE_SECURE:false}
app.email.delivery=${EMAIL_DELIVERY:console}
