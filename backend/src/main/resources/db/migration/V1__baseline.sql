-- Initial Flyway baseline migration
-- Domain tables (users, jobs, resumes, skills, applications) will be introduced in future versioned migrations.
-- This harmless statement establishes a valid baseline that Flyway can execute and record in flyway_schema_history.

SELECT 1;
